# Graph-Editor

GUI application which allows the user to build and modify an oriented graph.
The GUI was implemented using Swing and the MVC pattern.
The application also allows the user to save and load a graph, both functionalities being done in a custom format.
